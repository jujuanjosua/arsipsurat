<%= simple_form_for([@surat, @surat.dispos.build], remote: true) do |f| %>
	
<div class = "field">
	<div class = "control">
		<%= f.input :dispo, input_html: { class: 'textarea' }, wrapper: false, label_html: { class: 'label' } %>
	</div>
</div>

<%= f.button :submit, 'Leave a dispo', class:'button is-info' %>
<% end %>